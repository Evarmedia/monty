#include "monty.h"

/**
 * nop - nop does Nothing.
 * @head_stack: a pointer to the head_stack struct
 * @counter_numb: the number of each line in the file
 */

void nop(stack_t **head_stack, unsigned int counter_numb)
{
	(void) *head_stack;
	(void) counter_numb;
}
